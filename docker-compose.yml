version: '3.9'
services:
  redists:
    image: redislabs/redistimeseries:1.6.17
    restart: unless-stopped
    ports:
    - "64379:6379"
    volumes:
    - "redis-data:/data:delegated"
volumes:
  redis-data:
